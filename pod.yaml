apiversion: v1
kind: Pod
metadata:                       //----------->Details of the POD
  name: hello-pod
  labels:
     app: webserver
spec:                               //------------>Details of the Container inside the POD
  containers:
  - name: hello-ctr
    image: jenkins
    ports:
    - containerPort: 80
